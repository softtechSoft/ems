<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.softtech.mapper.MainMapper">
	<select id="queryOfcfunction" parameterType="string" resultType="Ofcfunction">
		select * from ems.ofcfunction where (authority=#{authority} or authority='2') and deleteFlg='0' order by displayNo
	</select>
</mapper>
